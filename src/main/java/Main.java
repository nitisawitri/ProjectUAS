import frame.DataMahasiswaFrame;
import frame.loginframe;
import helpers.koneksi;

public class Main {
    public static void main(String[] args) {
       //  koneksi.getConnection();
         loginframe viewFrame = new loginframe();
         viewFrame.setVisible(true);
    }
}
